#!/bin/bash

# 脚本路径
SCRIPT_PATH="/path/to/x-ui"

# 切换到脚本所在目录
cd $SCRIPT_PATH || { echo "Failed to change directory to $SCRIPT_PATH"; exit 1; }

# 启动 x-ui 并将输出重定向到 x-ui.log
sudo nohup ./x-ui > x-ui.log 2>&1 &

# 打印消息，表示脚本已启动
echo "x-ui has been started in the background. Check x-ui.log for output."
